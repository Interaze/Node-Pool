- name: configure ansible-pull
  hosts: all
  roles:
    - role: ansible-pull
      ansible_pull_url: ssh://git@github.com:Interaze/Node-Pool.git

- hosts: ubuntu
  tasks:
    - debug:
        msg: "I'm in group 'ubuntu'"
  roles:
    - role: ubuntu-install-dependencies
    - role: ubuntu-replicate-environment
    - role: ubuntu-run-environment

- hosts: upicluster
  tasks:
    - debug:
        msg: "I'm in group 'upicluster'"

- hosts: onlybar
  tasks:
    - debug:
        msg: "I'm in group 'onlybar'"

