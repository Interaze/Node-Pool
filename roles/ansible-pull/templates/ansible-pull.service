{{ ansible_managed|comment }}

[Unit]
Description=system configuration upgrade
After=network-online.target
 
[Service]
User=ubuntu
StandardOutput=file:/var/log/spot-node/stdo.log
ExecStart=/bin/sh -c 'ansible-pull -U "{{ ansible_pull_url }}" -i "$(uname -n),"'
TimeoutStopSec=600
